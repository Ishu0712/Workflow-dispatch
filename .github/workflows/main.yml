name: Hello World Workflow
on: workflow_dispatch

jobs:
  my_first_job: 
    name: My first job
    runs_on: ubuntu-latest
    steps:
     - name: Greeting
       run: echo "Hello world from my first Github Action worklflow"
   
    
